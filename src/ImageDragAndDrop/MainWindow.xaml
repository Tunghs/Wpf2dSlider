<Window x:Class="ImageDragAndDrop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ImageDragAndDrop"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="10"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid x:Name="Grid0" Background="Beige" Tag="0"
              AllowDrop="True">
            <Image/>
        </Grid>

        <Grid Grid.Column="2" x:Name="Grid1"  Background="Beige" Tag="1"
              AllowDrop="True">
            <Image/>
        </Grid>

        <Grid Grid.Row="2" x:Name="Grid2"  Background="Beige" Tag="2"
              AllowDrop="True">
            <Image/>
        </Grid>

        <Grid Grid.Row="2" Grid.Column="2" x:Name="Grid3" Background="Beige" Tag="3"
              AllowDrop="True">
            <Image/>
        </Grid>
    </Grid>
</Window>
